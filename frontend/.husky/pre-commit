# #!/usr/bin/env sh
# . "$(dirname -- "$0")/_/husky.sh"

# npx lint-staged

#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Husky pre-commit hook uruchomiony!"
npx lint-staged
echo "✅ Husky pre-commit zakończony!"
