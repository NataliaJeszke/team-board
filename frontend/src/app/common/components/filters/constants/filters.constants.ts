export enum FilterType {
  SELECT = 'select',
  MULTI_SELECT = 'multi-select',
  DATE = 'date',
  DATE_RANGE = 'date-range',
  TEXT = 'text',
}

export const DEFAULT_BUTTON_RESET = 'common.components.filters.actions.reset';
